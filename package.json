{
  "name": "team-project-client",
  "version": "0.0.1",
  "description": " Build support for the client-side of CS326 projects.",
  "author": "Anh Bui <abc@cs.umass.edu>",
  "license": "MIT",
  "scripts": {
    "start": "(cd server && node src/server.js)",
    "serve": "webpack-dev-server --content-base build",
    "heroku-prebuild": "(cd client && npm install && npm install --only=dev && npm run build); (cd server && npm install)",
    "heroku-postbuild": "",
    "watch": "webpack --watch"
  },
  "cacheDirectories": ["client/node_modules", "server/node_modules"]
}
